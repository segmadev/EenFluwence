import 'package:flutter/material.dart';

class UWalletScreen extends StatelessWidget {
  const UWalletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("User Wallet Page"),
    );
  }
}
